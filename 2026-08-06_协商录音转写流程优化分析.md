# 协商录音转写流程优化分析

> 形成日期：2026 年 8 月 6 日  
> 适用范围：以 2026 年 8 月 5 日与来瑞的两人协商录音为例，分析当前转写、说话人归属和阅读整理流程，并提出在 [FunASR](https://github.com/modelscope/FunASR) 与 [FunASR_e2e](https://github.com/hejch20108/FunASR_e2e) 基础上的优化建议。原始处理文件已在项目外归档，本仓库保留原始 WAV 与整理稿。

## 一、结论摘要

本次对 2026 年 8 月 5 日录音的整理稿复核能够将大量“话语与人物错配”的内容校正到较高准确度，关键不在于单独更换 ASR 模型，而在于把以下信息联合使用：

1. 原始音频及其时间轴；
2. FunASR JSON 中的句段、字词时间戳和匿名说话人编号；
3. 双方稳定的谈话角色、主张事项和语义立场；
4. 问答、反驳、抢话等对话轮次关系；
5. 对无法可靠判断的片段明确标记“待回听原音核验”，而不强行归属。

当前 `polished.txt` 的提升，本质上属于一次**保守的结构化说话人复核**：对高置信度的句子重新归属、拆分原本被合并的交替发言；对重叠发言和短促插话保留不确定性。

后续最高收益的改进不是继续优化“润色”，而是：

> **固定已知说话人数、在说话人边界处切分字词时间戳、不删除关键短答、自动标记高风险片段，并将身份映射与 LLM 复核变为可追溯的独立步骤。**

## 二、本次整理稿校正的方法

### 1. 区分材料层级

| 层级 | 文件 | 用途与边界 |
|---|---|---|
| 原始证据 | `协商录音/录音音频/08月05日_来瑞_港之龙.wav` | 最终核验依据；不得被处理稿替代。 |
| 结构化原始转写 | `08月05日_来瑞_港之龙.json` | 保留全文、时间戳和 `sentence_info`，是重建段落与说话人边界的主要技术依据。 |
| 清洗阅读稿 | `08月05日_来瑞_港之龙_cleaned.txt` | 已清理口语杂质、合并同标签相邻段，便于阅读，但可能放大上游错标。 |
| 润色阅读稿 | `08月05日_来瑞_港之龙_polished.txt` | 已作语句整理，适用于检索和理解；不能替代音频或 JSON。 |

JSON 的顶层实际包含：

```text
key
text
timestamp
sentence_info
```

其中 `sentence_info` 的句段起止时间、文本和匿名说话人标记，是后续重新拆分与归属的重要基础。

### 2. 先建立人物身份锚点

FunASR 的 `spk0`、`spk1` 只表示本段录音中聚类出来的不同声纹类别，不等同于固定姓名。复核时先根据开头称呼、长期稳定立场和话语内容建立身份锚点：

| 识别特征 | 更可能的说话人 |
|---|---|
| “我算的”“我不认可”“我的诉求”“我可以不追缴社保”“我可以打九折”；主张 14600 元、N/N+1、年假、欠薪、公积金 | 何景城 |
| “公司”“协商”“匹配项目”“不在项目状态”“公司有合法依据”“继续推进安置”；以低工资、失业金、时间精力劝说一次性处理 | 来瑞 |

这不是用语义替代音频，而是用角色稳定性检查和纠正声纹聚类、分句造成的显著错配。

### 3. 按对话轮次拆分，而不是整段换标签

原有输出可能将实际交替的发言合在同一标点句中，例如：

```text
何景城：我主张……
来瑞：但公司……
何景城：不是，我的意思是……
来瑞：那你……
```

却输出为：

```text
说话人1：
我主张……但公司……不是，我的意思是……那你……
```

复核中以时间轴、上下文和问答关系拆为多个子段；例如将“来瑞询问补偿基数—何景城说明 14600 元口径—来瑞否定算法”的连续交锋重新分开。

### 4. 对低置信度内容保留边界

以下情形不强行给出人物归属：

- 两人同时讲话；
- 短答被前后长句覆盖；
- 原始 ASR 文本残缺；
- 说话人声纹边界与语义句界明显冲突；
- 仅凭上下文存在多个合理解释。

这类内容标记为“双方交叠，待回听原音核验”。这种处理比为了版面整齐而错误归属更适合劳动争议材料。

## 三、当前 FunASR_e2e 流程

当前项目是对 FunASR 的编排层，主要流水线可概括为：

```text
录音文件
  ↓
FSMN-VAD：检测语音区间
  ↓
SeACo Paraformer：ASR 与字词级时间戳
  ↓
CT-Transformer：标点恢复与句段生成
  ↓
CAM++：声纹 embedding 与聚类
  ↓
FunASR JSON
  ↓
本地规则清洗、同说话人合并
  ↓
cleaned.txt
  ↓
LLM 轻度润色正文
  ↓
polished.txt
```

当前涉及的关键文件包括：

- `FunASR_e2e/settings.yaml`
- `FunASR_e2e/scripts/run_funasr_full_pipeline.py`
- `FunASR_e2e/scripts/postprocess_funasr_transcript.py`
- `FunASR_e2e/prompt/polish_prompt_template.txt`
- `FunASR_e2e/prompt/drop_words.txt`
- `FunASR_e2e/prompt/filler_words.txt`
- `FunASR_e2e/prompt/repeated_words.txt`

当前主要模型：

| 环节 | 当前模型或机制 |
|---|---|
| VAD | `iic/speech_fsmn_vad_zh-cn-16k-common-pytorch` |
| ASR | `iic/speech_seaco_paraformer_large_asr_nat-zh-cn-16k-common-vocab8404-pytorch` |
| 标点 | `iic/punc_ct-transformer_cn-en-common-vocab471067-large` |
| 说话人声纹 | `iic/speech_campplus_sv_zh-cn_16k-common` |
| 说话人聚类 | FunASR `ClusterBackend` |
| 阅读稿润色 | 默认 `qwen3-max` |

## 四、原流程出现串词、错标的根本原因

### 1. VAD 不等于说话人切换检测

VAD 只能判断是否存在语音，不能判断是谁在说。两人无缝接话、短暂停顿或抢话时，可能落在同一个连续语音区间。

### 2. 声纹聚类不是身份识别

CAM++ 通过短窗口提取声纹并聚类，只能输出匿名类别。`spk0` 在不同录音中未必总是同一个人；同一人也可能因距离、音量、情绪、电话外放或噪声被拆成多个类别。

### 3. 标点句界和说话人边界相互独立

标点模型按语言文本分句，声纹模型按声学窗口判断身份。两者没有天然的一一对应关系。

### 4. 整句按最大重叠分配一个人

当前上游逻辑会为整个标点句选择重叠时间最多的说话人。结果是：

- 长句说话人会吞掉另一方的“对”“不是”“可以”等短答；
- 句内抢话被强行归给一个人；
- 交替发言被展示为一个人的连续表述；
- 边界不清时可能回落给默认匿名说话人。

### 5. 约 1.5 秒的声纹窗口不适合处理短插话和重叠语音

短于约一秒的回应、打断或同时发言，可能与前后声纹混合。聚类不是语音源分离，无法完整恢复“同一时刻两个人分别说了什么”。

### 6. 后处理会放大上游错误

如果两个相邻句段已被错标为同一人，清洗程序会在间隔较短时将其继续合并，形成更长、看似更连贯但实际串词的段落。

### 7. 当前 LLM 润色阶段无法修复人物标签

当前实现仅把正文交给 LLM 润色，再把原始时间戳和说话人头拼回。这能避免 LLM 擅改结构，但也意味着 LLM 看不到音频与人物边界，无法校正上游错标。

## 五、优化建议与优先级

### 优先级一：明确人数时传入 `preset_spk_num`

两人协商录音可在配置中增加：

```yaml
funasr:
  preset_spk_num: 2
```

并传入：

```python
model.generate(
    input=str(audio_path),
    preset_spk_num=2,
)
```

作用是减少自动聚类将两人估成一人、三人或更多人的情况。

注意：不能全局无条件固定为两人。存在第三人、电话外放或旁人插话的录音，应根据真实场景改为 3 人或保留自动模式。

### 优先级二：按说话人边界切分字词时间戳

这是解决“同一句里含双方内容”的最重要代码级优化。

当前逻辑：

```text
标点句
  ↓
比较与各 speaker 时间区间的重叠
  ↓
整句给一个 speaker
```

建议改为：

```text
字词时间戳 + speaker 时间区间
  ↓
发现 speaker 边界
  ↓
在边界处分开字词
  ↓
形成多个说话人子句
  ↓
再做轻度标点、清洗与合段
```

边界附近建议设置约 100—300ms 的待核容差；压在边界上的字词不应自动断言归属。

### 优先级三：增加高风险片段标记

目标不是自动定稿 100%，而是将人工回听集中到最有风险的 5%—15%。建议自动标记：

- 说话人段落短于 1 秒；
- 一个句段覆盖多个 speaker 时间区间；
- speaker 边界前后 300ms 内存在文本；
- 连续快速切换说话人；
- 发生重叠语音；
- 涉及金额、日期、工龄、N/N+1、工资、补偿、解除、年假、社保、公积金；
- 两个识别模型文本不一致；
- 清洗阶段删除了关键短答。

建议在结构化中间结果中保留：

```json
{
  "start": 96240,
  "end": 100880,
  "spk": "来瑞",
  "text": "……",
  "review_flags": [
    "speaker_boundary_inside_sentence",
    "contains_amount",
    "short_interruption"
  ]
}
```

### 优先级四：调整危险的清洗规则

劳动争议录音中，“对”“好”“不是”“可以”等短答可能是对金额、方案、事实或承诺的确认，不应自动删除。

建议：

1. 从 `drop_words.txt` 中移除“对”“好”；
2. 在未删词的证据转写层保留“嗯、啊、呃”等；
3. 仅在阅读版中弱化明显无实义的语气词；
4. 为所有自动删除保留日志。

建议固定输出四层：

```text
1. 原始 WAV
2. FunASR 原始 JSON
3. 未删词的证据转写稿
4. cleaned / polished 阅读整理稿
```

### 优先级五：加入案件专用热词

可为单次案件配置少量高价值热词，例如：

```text
何景城
来瑞
徐云平
广州市讯方信息科技有限公司
港之龙
华为云
PP 项目
退项
不在项目
薪资确认单
经济补偿金
N 加一
公积金
社保
年休假
仲裁
14600
```

热词主要用于减少人名、公司名、项目名、岗位和固定术语的误识别。金额、日期、补偿月数不宜仅靠热词或模糊替换自动定稿，仍应人工核验。

### 优先级六：将 CAM++ 与 ERes2NetV2 做 A/B 测试

可用以下模型作为对照：

```yaml
spk_model: iic/speech_eres2netv2_sv_zh-cn_16k-common
```

不要预设某一个一定更好。应选取正常轮流发言、短答、抢话、低音量、远近距离变化、混响、电话外放等代表性片段，比较：

- 句段人物归属正确率；
- 人名、金额、日期和术语准确率；
- 重叠与短插话的风险标记效果；
- 人工回听耗时。

### 优先级七：增加真实身份映射

匿名 `spk0/spk1` 应通过声纹注册映射为具体人员：

```text
双方各选若干段干净、无重叠、身份已知的语音
  ↓
提取双方 enrollment embedding
  ↓
与本次录音的聚类中心计算余弦相似度
  ↓
差异显著时映射为何景城 / 来瑞
  ↓
差异不显著时保留匿名标签并人工确认
```

这应视为“FunASR 原生声纹向量 + 自定义身份映射”，并非一键功能。

### 优先级八：新增保守、可审计的 LLM 说话人复核层

建议将 LLM 从仅润色正文升级为独立的中间步骤：

```text
原始 JSON
  ↓
结构化 speaker review
  ↓
speaker_review.json
  ├─ 原文本
  ├─ 原 speaker 标签
  ├─ 建议切分点
  ├─ 建议人物归属
  ├─ 置信度
  ├─ 依据
  └─ 是否必须人工回听
  ↓
人工确认
  ↓
生成 cleaned / polished 阅读稿
```

提示词应要求：

- 不得增写、删改或概括原话；
- 不得修改数字、日期、主体名称；
- 只能拆分原句、建议归属或标记待核；
- 低置信度必须输出待回听；
- 保留原始时间范围、segment ID 和所有改动日志。

### 优先级九：锁定版本并生成处理清单

建议每次处理保存 manifest：

```json
{
  "source_audio_sha256": "……",
  "funasr_commit": "……",
  "asr_model": "……",
  "vad_model": "……",
  "spk_model": "……",
  "preset_spk_num": 2,
  "hotword_file": "case-2026-08-05.txt",
  "postprocess_config_sha256": "……",
  "llm_model": "……",
  "prompt_version": "……"
}
```

这能够在以后重跑同一录音时解释结果差异，增强证据整理的可追溯性。

## 六、推荐工作流

### 1. 录音笔单声道混录

```text
原始 WAV（只读保存）
  ↓
生成 16kHz PCM WAV 工作副本
  ↓
FunASR：VAD + Paraformer + 标点 + 声纹聚类
  ↓
已知双人时设 preset_spk_num=2
  ↓
生成 JSON 与未删词转写
  ↓
按 speaker 边界切分字词时间戳
  ↓
短答、抢话、重叠、金额日期自动标记待核
  ↓
声纹 enrollment 映射真实身份
  ↓
LLM 仅进行保守的结构化 speaker review
  ↓
人工回听高风险片段
  ↓
生成 cleaned / polished 阅读稿
```

### 2. 如录音笔文件是双声道

如左右声道主要分别采集两人声音，应优先拆分声道，再分别识别并按原始绝对时间合并。直接将立体声音频输入常规 FunASR 流程可能被平均为单声道，从而失去声道区分优势。

双声道方案：

```text
原始立体声录音
  ↓
拆分左、右声道
  ↓
各自转为 16kHz 单声道 WAV
  ↓
每个声道分别做 VAD、ASR、标点
  ↓
按绝对时间轴合并，声道直接映射身份
  ↓
重叠保留，不强制二选一
```

## 七、暂不建议的方向

不建议仅针对当前单个案件直接训练或微调 ASR 模型，原因包括：

- 可用的高质量逐字标注数据通常不足；
- 容易过拟合特定人物、录音笔和房间环境；
- 目前最大瓶颈在换人边界、短插话和重叠语音，而非单纯字词识别；
- 固定人数、热词、边界切分、复核标记和身份映射的收益更直接、成本更低。

## 八、建议的实施顺序

1. 移除自动丢弃词中的“对”“好”，保留未删词证据转写层；
2. 为已知双人录音增加 `preset_spk_num=2`；
3. 增加案件专用热词；
4. 用 CAM++ 与 ERes2NetV2 在真实录音片段上 A/B 测试；
5. 实现按说话人边界切分字词时间戳；
6. 为短答、重叠和跨说话人句段增加复核标记；
7. 增加声纹注册与真实身份映射；
8. 引入保守、可审计的 LLM speaker review；
9. 固定 FunASR、模型、依赖、提示词和配置版本。

## 九、材料使用提示

本分析属于技术流程和材料整理建议，不改变本案事实认定。所有 `cleaned.txt`、`polished.txt`、说话人修正稿及模型输出均应标明其整理性质；在仲裁或其他正式程序中引用具体原话、金额、日期、解除或协商意思表示前，仍应回听原始 WAV 并保留对应时间戳。
